import { Hero } from "@/components/hero/Hero";

/**
 * Página principal. Las secciones se van añadiendo aquí en el orden en que
 * aparecen en la web; la numeración de cada una es su posición en la página.
 */
export function Home() {
  return (
    <>
      <Hero />
    </>
  );
}
